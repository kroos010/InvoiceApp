import { CustomerInvoiceSearchPipe } from './customer-invoice-search.pipe';

describe('CustomerInvoiceSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new CustomerInvoiceSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
